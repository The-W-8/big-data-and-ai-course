# Agent、大模型上下文、Skill 三者关系

> 本文说明三个概念的关系：**Agent（智能体）、大模型上下文（Context / Context Window）、Skill（AI 技能）**。
> 一句话总纲：**上下文是 Agent 的"工作记忆"，决定它每一轮能看见什么；Skill 是把可复用任务知识沉淀下来、按需注入上下文的"外挂手册"；Agent 则是在这两者支撑下自主循环干活的系统。**

## 1. 三者定位总览

| 维度 | Agent | 大模型上下文 | Skill |
| --- | --- | --- | --- |
| 本质角色 | 完整的应用系统 | 模型推理的输入空间 | 标准化的知识包 |
| 解决什么问题 | 让 AI 自主完成多步任务 | 决定单次推理的信息范围 | 把任务流程知识沉淀复用 |
| 存在形态 | 框架/产品（LLM + 工具 + 循环） | Transformer 的机制属性 | 一个带 SKILL.md 的文件夹 |
| 生命周期 | 任务全程持续运行 | 每次推理重新拼接 | 长期沉淀，触发时加载 |
| 相互关系 | 依赖上下文运转，可调用 Skill | 承载 Agent 的感知，也承载 Skill 的注入 | 按需进入上下文，增强 Agent |

## 2. 结构图（Mermaid）

```mermaid
flowchart TD
    User([用户提出目标]) --> Agent

    subgraph AgentSystem["Agent 系统（循环执行）"]
        Agent[Agent 大脑 = LLM]

        subgraph Context["上下文（每次推理的完整输入）"]
            SP[系统提示词]
            Hist[对话历史 / 工具调用结果]
            SkillInject[被触发的 Skill 指令]
            RAG[检索注入的资料]
        end

        Agent -->|"读取"| Context
        Agent -->|"决策：下一步做什么"| Tools[工具调用<br/>搜索 / 代码 / 文件 / API]
        Tools -->|"环境反馈写回"| Hist
    end

    SkillStore[(".workbuddy/skills/<br/>沉淀的 Skill 知识包<br/>（平时不占上下文）") -.->|"任务匹配时<br/>渐进式加载"| SkillInject

    Agent -->|"循环直到任务完成"| Done([交付结果])
```

**读图要点：**

- Agent 的每一次决策，都是在读取整个上下文之后做出的——上下文是 Agent 与世界打交道的唯一通道；
- 工具执行的反馈会作为新的上下文写回，驱动下一轮决策（"LLM 在循环中根据环境反馈使用工具"）；
- Skill 平时静静躺在文件系统里，只有当任务匹配时才被注入上下文，用完即走，不长期占地方。

## 3. 上下文如何影响 Agent 运行

上下文对 Agent 的影响是**机制级**的，可以从四个方面理解：

**（1）上下文是 Agent 的"工作记忆"，决定感知范围。**
Agent 看不见上下文之外的任何东西：目标、计划、工具反馈都必须被写进上下文才能参与决策。一旦信息被截断挤出窗口（多轮长任务中极常见），Agent 对早期内容就"彻底失忆"，可能出现重复劳动或目标漂移。

**（2）上下文的质量直接决定决策质量。**
注入的检索资料不准、工具返回结果冗长噪杂，都会稀释模型注意力。研究表明模型对长文本中间信息的利用率显著偏低（Lost in the Middle）——所以工程上要控制注入内容的数量与位置，"少而准"优于"多而全"。

**（3）上下文容量是 Agent 的硬性预算。**
窗口大小（如 128K token）由模型决定，系统提示、对话历史、工具定义、检索资料、Skill 指令**共享**这块预算。Agent 每多挂一个工具、多注入一段资料，留给"真正的任务内容"的空间就少一分——这就是为什么不能把所有知识都塞进上下文，也是 Skill 存在的直接原因。

**（4）长任务的上下文管理策略决定 Agent 能跑多远。**
生产级 Agent 普遍配套上下文管理手段：历史压缩摘要、外部记忆（向量库）按需召回、关键信息回写等。可以说，**上下文管理水平 = Agent 的续航能力**。

## 4. Skill 如何沉淀可复用任务知识

Skill 解决的是"知识资产化"问题，可以从三个层面理解：

**（1）把一次性 Prompt 升级为可维护的知识资产。**
临时提示词用完即弃、无法追溯；Skill 是文件系统里的一个文件夹（SKILL.md + 可选脚本/资料），可以被 Git 版本管理、被团队共享、被持续迭代——"任务知识"从聊天记录变成了代码仓库里的资产。

**（2）渐进式披露：对上下文友好的加载方式。**
Skill 的三级加载机制与上下文天然配合：平时只有百来 token 的元数据常驻；任务匹配时才把正文读进上下文；参考资料、脚本在真正用到时才读取。这意味着可以沉淀几十上百个 Skill 而几乎不挤占 Agent 的"工作记忆"——**沉淀的知识越多，并不等于上下文越挤**。

**（3）让 Agent 跨会话、跨人员稳定复现"专家流程"。**
没有 Skill 时，"AI 按公司规范写周报"依赖于每个员工各自粘贴的提示词，质量参差；有了 Skill，只要任务匹配，任何会话中的 Agent 都会加载同一份标准流程，输出口径一致。Skill 本质上是把"老员工脑子里的隐性流程"外化为"Agent 可执行显性知识"，是组织级 prompt 资产的沉淀形态。

## 5. 三者协同的完整闭环

一个典型任务（"按团队规范整理本周项目周报"）在三者协同下的运转方式：

1. **Skill 沉淀**（事前）：团队把周报规范写成 `weekly-report` Skill 放入 skills 目录——知识完成资产化，平时不占上下文；
2. **上下文组装**（事中）：用户请求到达，Agent 依据 Skill 元数据判断匹配，将 SKILL.md 正文注入当前上下文，与系统提示、对话历史、检索资料一起构成本轮推理的完整输入；
3. **Agent 循环**（事中）：Agent 在上下文支撑下规划、调用工具（读取项目进度、统计代码提交）、将工具反馈写回上下文、迭代决策，直到产出符合规范的周报；
4. **知识回流**（事后）：若执行中发现规范需要修订，更新 SKILL.md 即可，下一次任何人触发时自动生效——知识沉淀完成闭环。

**总结：上下文是"流动的血液"（决定 Agent 每轮看见什么），Skill 是"仓库里的手册"（把流程知识按需输送进上下文），Agent 是"整个有机体"（在血液供养下循环工作）。三者共同构成"知识沉淀 → 按需注入 → 自主执行"的现代 AI 应用范式。**

---

参考（链接详见各学习笔记）：

- Lilian Weng《LLM Powered Autonomous Agents》：https://lilianweng.github.io/posts/2023-06-23-agent/
- Anthropic《Building Effective Agents》：https://www.anthropic.com/engineering/building-effective-agents
- 《Lost in the Middle》论文：https://arxiv.org/abs/2307.03172
- Anthropic Agent Skills 官方博客：https://claude.com/blog/skills
